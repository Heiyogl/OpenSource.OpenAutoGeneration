package ${source_package_service};

import org.springframework.stereotype.Component;

@Component
public class ${className?cap_first}Service extends ${source_package_service_build}.${className?cap_first}ServiceBuild {
	
}
