package ${source_package_mapper};

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface ${className?cap_first}Mapper extends ${source_package_mapper_build}.${className?cap_first}MapperBuild {

}

